{% trans_default_domain 'sfs_user' %}
{% extends '@SfsUser/admin_layout.html.twig' %}

{% block content %}
    <h2>{{ administrator.name }} {{ administrator.surname }}</h2>

    <div class="text-right">
        <a href="#" class="btn btn-outline-secondary">Demote Admin</a>
        <a href="#" class="btn btn-outline-secondary">Edit</a>
        <a href="#" class="btn btn-outline-secondary">Reset password</a>
        <a href="#" class="btn btn-outline-secondary">Manual password</a>
        <a href="#" class="btn btn-outline-secondary">Delete</a>
    </div>

    <p><strong>name:</strong> {{ administrator.name }}</p>
    <p><strong>surname:</strong> {{ administrator.surname }}</p>
    <p><strong>username:</strong> {{ administrator.username }}</p>
    <p><strong>email:</strong> {{ administrator.email }}</p>
    <p><strong>lastLogin:</strong> {{ administrator.lastLogin ? administrator.lastLogin|date('H:i d-m-Y') : 'not yet' }}</p>
    <p><strong>role:</strong> {{ administrator.roles|join(', ') }}</p>

    <div class="row">
        <div class="col-4">
            <div class="card">
                <div class="card-header">
                    <h4>Accounts</h4>
                </div>
                <div class="card-body">
                    <p><i>TODO</i></p>
                </div>
            </div>
        </div>
        <div class="col-4">
            <div class="card">
                <div class="card-header">
                    <h4>Latest access</h4>
                </div>
                <div class="card-body">
                    <p><i>TODO</i></p>
                </div>
            </div>
        </div>
        <div class="col-4">
            <div class="card">
                <div class="card-header">
                    <h4>Emails sent</h4>
                </div>
                <div class="card-body">
                    <p><i>TODO</i></p>
                </div>
            </div>
        </div>
    </div>
{% endblock content %}