{% trans_default_domain 'sfs_user' %}
{% extends '@SfsUser/preferences_layout.html.twig' %}

{% block content %}
    <div class="bg-white p-5 w-50">
        <h1 class="h4 mb-3 fw-normal">{{ 'change_email.title'|trans }}</h1>

        {{ form_start(form) }}
        {{ form_rest(form) }}
        <input type="submit" value="{{ 'change_email.actions.submit.button'|trans }}" class="btn btn-primary btn-block" />
        {{ form_end(form) }}
    </div>
{% endblock content %}